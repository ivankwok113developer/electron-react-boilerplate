import React from 'react';

const BottomBar: React.FC = () => {
  return (
    <div className="h-12 bg-purple-500 w-full">
      {/* Bottom bar content */}
    </div>
  );
};

export default BottomBar;
